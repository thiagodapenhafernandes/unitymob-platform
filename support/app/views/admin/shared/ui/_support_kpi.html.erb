<div class="ax-mini-kpi"><span><%= label %></span><strong><%= value %></strong><small class="<%= detail == 'Fora do prazo' ? 'ax-mini-kpi--late' : ['No prazo','Dentro do prazo'].include?(detail) ? 'ax-mini-kpi--ontime' : '' %>"><%= detail %></small></div>
