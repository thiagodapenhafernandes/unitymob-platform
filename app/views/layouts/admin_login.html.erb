<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>Login - Salute Imóveis Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= render 'layouts/no_zoom' %>
    <%= render 'layouts/pwa_meta' %>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
      :root {
        --primary: #1e3a8a;
        --secondary: #6366f1;
        --accent: #f59e0b;
        --glass: rgba(255, 255, 255, 0.85);
        --glass-border: rgba(255, 255, 255, 0.2);
      }

      body.admin-login {
        background: url('/assets/admin-login-bg.png') no-repeat center center fixed;
        background-size: cover;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: 'Inter', sans-serif;
        position: relative;
      }

      body.admin-login::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(30, 58, 138, 0.8) 0%, rgba(99, 102, 241, 0.6) 100%);
        backdrop-filter: blur(4px);
        z-index: 1;
      }
      
      .login-container {
        position: relative;
        z-index: 10;
        width: 100%;
        max-width: 500px;
        padding: 24px;
        animation: fadeInUp 0.8s ease-out;
      }

      @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }
      
      .login-card {
        background: var(--glass);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--glass-border);
        border-radius: 24px;
        box-shadow: 0 30px 60px -14px rgba(0, 0, 0, 0.55);
        overflow: hidden;
      }
      
      .login-header {
        padding: 44px 40px 28px;
        text-align: center;
        border-bottom: 2px solid rgba(0,0,0,0.05);
      }
      
      .login-header .logo-icon {
        width: 64px;
        height: 64px;
        background: white;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 20px;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        color: var(--primary);
      }

      .login-header h1 {
        font-size: 26px;
        font-weight: 800;
        margin: 0;
        color: #111827;
        letter-spacing: -0.5px;
      }
      
      .login-header p {
        margin-top: 5px;
        color: #6b7280;
        font-size: 14px;
        font-weight: 500;
        text-transform: uppercase;
        letter-spacing: 1.5px;
      }
      
      .login-body {
        padding: 34px 40px 38px;
      }
      
      .form-group {
        margin-bottom: 18px;
      }
      
      .form-label {
        font-weight: 600;
        color: #374151;
        margin-bottom: 8px;
        font-size: 13px;
        display: block;
      }
      
      .form-control {
        height: 50px;
        border: 1.5px solid #e5e7eb;
        border-radius: 12px;
        padding: 10px 14px;
        font-size: 15px;
        font-weight: 500;
        transition: all 0.3s;
        background: white;
      }
      
      .form-control:focus {
        border-color: var(--secondary);
        box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.15);
        outline: none;
      }
      
      .btn-login {
        width: 100%;
        height: 52px;
        background: #111827;
        border: none;
        border-radius: 12px;
        color: white;
        font-weight: 700;
        font-size: 16px;
        transition: all 0.3s;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        margin-top: 10px;
      }

      .input-group {
        border: 1.5px solid #e5e7eb;
        border-radius: 12px;
        background: #fff;
        transition: all 0.3s;
      }

      .input-group:focus-within {
        border-color: var(--secondary);
        box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.15);
      }

      .input-group-text {
        border: 0;
        background: transparent;
        color: #6b7280;
        padding-left: 14px;
        padding-right: 8px;
      }

      .input-group .form-control {
        border: 0 !important;
        border-radius: 0 12px 12px 0 !important;
        box-shadow: none !important;
        padding-left: 6px;
      }

      .btn-login:hover {
        background: #1f2937;
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2);
      }
      
      .btn-login:active {
        transform: translateY(0);
      }

      .form-check {
        display: flex;
        align-items: center;
        gap: 10px;
        margin: 8px 0 24px;
      }
      
      .form-check-input {
        width: 18px;
        height: 18px;
        cursor: pointer;
        border: 1.5px solid #d1d5db;
        border-radius: 4px;
      }
      
      .form-check-label {
        font-size: 14px;
        font-weight: 500;
        color: #4b5563;
        cursor: pointer;
      }
      
      .forgot-link {
        display: block;
        text-align: center;
        margin-top: 18px;
        color: #6b7280;
        text-decoration: none;
        font-size: 13px;
        font-weight: 600;
        transition: color 0.2s;
      }

      .login-title {
        font-size: 2rem;
        font-weight: 800;
        color: #111827;
        letter-spacing: -0.03em;
        margin: 0 0 22px;
      }
      
      .forgot-link:hover {
        color: var(--primary);
      }
      
      .alert {
        border-radius: 12px;
        margin-bottom: 25px;
        padding: 15px 20px;
        font-size: 14px;
        font-weight: 500;
        border: none;
      }

      .alert .btn-close {
        padding: 0.95rem;
      }
      
      .alert-danger {
        background-color: #fee2e2;
        color: #991b1b;
      }

      .alert-success {
        background-color: #dcfce7;
        color: #166534;
      }

      @media (max-width: 576px) {
        .login-container {
          padding: 14px;
        }

        .login-header {
          padding: 34px 22px 24px;
        }

        .login-body {
          padding: 26px 22px 30px;
        }

        .login-title {
          font-size: 1.75rem;
          margin-bottom: 18px;
        }
      }
    </style>
  </head>

  <body class="admin-login">
    <div class="login-container">
      <div class="login-card">
        <div class="login-header">
          <div class="logo-icon">
            <i class="bi bi-shield-lock-fill" style="font-size: 32px;"></i>
          </div>
          <h1>Salute Imóveis</h1>
          <p>Acesso Restrito</p>
        </div>
        
        <div class="login-body">
          <%= render 'shared/flash_messages' if flash.any? %>
          <%= yield %>
        </div>
      </div>
      
      <p class="text-center mt-4 text-white opacity-75" style="font-size: 12px; font-weight: 500;">
        &copy; <%= Time.current.year %> Salute Imóveis - Todos os direitos reservados.
      </p>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= render 'layouts/pwa_service_worker' %>
  </body>
</html>
