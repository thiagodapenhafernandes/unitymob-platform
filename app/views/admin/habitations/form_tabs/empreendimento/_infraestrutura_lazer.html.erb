<% infrastructure_actions = capture do %>
  <button type="button" class="ax-btn ax-btn--ghost ax-btn--sm"
          data-attribute-manager-context-value="habitation"
          data-attribute-manager-category-value="infrastructure"
          data-attribute-manager-field-name-value="habitation[infra_estrutura][]"
          data-attribute-manager-modal-id-value="attributeManagerModal"
          data-action="click->attribute-manager#open"
          data-field-lock-action="acao:gerenciar_infraestrutura"
          data-title="Gerenciar Infraestrutura"
          data-select-id="infra-checkbox-list">
    <i class="bi bi-gear ax-ico"></i>Gerenciar
  </button>
<% end %>

<%= ax_form_section(title: "Infraestrutura e lazer", eyebrow: "Checklist", icon: "tree", actions: infrastructure_actions) do %>
  <% current_infra = f.object.caracteristicas_predio %>
  <% category_options = habitation_category_checklist(f.object, "infrastructure", @external_features) %>
  <% infrastructure_options = category_options.values.flatten.uniq.sort %>
  <%= hidden_field_tag "habitation[infra_estrutura][]", "" %>
  <%= ax_chip_grid(id: "infra-checkbox-list", class_name: "ax-chip-grid--columns") do %>
    <% infrastructure_options.each do |feature| %>
      <% checked = current_infra.include?(feature) %>
      <label data-category-options="<%= json_escape(category_options.select { |_, values| values.include?(feature) }.keys.to_json) %>" class="custom-checkbox-card ax-chip-grid__item <%= 'is-checked' if checked %>">
        <input type="checkbox" name="habitation[infra_estrutura][]" value="<%= feature %>" data-exclusive-choice-group="<%= f.object.warehouse_exclusive_choice_group(feature) %>" <%= 'checked' if checked %>>
        <span title="<%= feature %>"><%= feature %></span>
      </label>
    <% end %>
  <% end %>
<% end %>
