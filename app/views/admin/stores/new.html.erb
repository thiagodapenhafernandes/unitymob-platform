<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="h3 mb-1 text-gray-800">
      <i class="bi bi-plus-square-fill text-primary me-2"></i>Nova loja
    </h1>
    <p class="text-muted small mb-0">Cadastre uma loja física com geofence, turnos e diretor.</p>
  </div>
  <%= link_to admin_stores_path, class: "btn btn-outline-secondary btn-sm" do %>
    <i class="bi bi-arrow-left me-1"></i>Voltar
  <% end %>
</div>

<%= render "form", store: @store %>
