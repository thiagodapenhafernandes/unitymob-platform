<article class="ax-metric-card <%= local_assigns[:class_name] %>" aria-label="<%= label %>"<%= raw %( data-tone="#{h(tone)}") if local_assigns[:tone].present? %>>
  <div class="ax-metric-card__header">
    <% if local_assigns[:icon].present? %>
      <span class="ax-metric-card__lead">
        <span class="ax-metric-card__icon" aria-hidden="true"><i class="bi bi-<%= icon %>"></i></span>
        <span class="ax-metric-card__label"><%= label %></span>
      </span>
    <% else %>
      <span class="ax-metric-card__label"><%= label %></span>
    <% end %>
    <% if local_assigns[:badge].present? %>
      <%= badge %>
    <% end %>
  </div>
  <div class="ax-metric-card__value"><%= value %></div>
  <% if local_assigns[:hint].present? %>
    <div class="ax-metric-card__hint"><%= hint %></div>
  <% end %>
  <% if local_assigns[:progress].present? %>
    <%= ax_progress(value: progress, label: "#{label}: #{[[progress.to_f, 0].max, 100].min}%") %>
  <% end %>
</article>
