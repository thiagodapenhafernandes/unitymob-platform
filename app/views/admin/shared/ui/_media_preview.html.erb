<%
  preview_classes = [
    "ax-media-preview",
    "ax-media-preview--#{variant}",
    "ax-media-preview--#{size}",
    class_name
  ].compact.join(" ")
%>

<%# Ao escolher um arquivo de imagem no corpo (ax_file_field), o quadro passa a mostrá-lo na hora (controller ax-media-preview). %>
<article class="<%= preview_classes %>" data-controller="ax-media-preview" data-action="change->ax-media-preview#pick">
  <% if label.present? %>
    <div class="ax-media-preview__label">
      <i class="bi bi-<%= icon %>" aria-hidden="true"></i>
      <span><%= label %></span>
    </div>
  <% end %>

  <div class="ax-media-preview__frame">
    <% if image.present? %>
      <%= image_tag image, class: "ax-media-preview__image" %>
    <% else %>
      <div class="ax-media-preview__empty">
        <i class="bi bi-image" aria-hidden="true"></i>
        <% if empty_text.present? %><span><%= empty_text %></span><% end %>
      </div>
    <% end %>
  </div>

  <% if body.present? %>
    <div class="ax-media-preview__body"><%= body %></div>
  <% end %>
</article>
