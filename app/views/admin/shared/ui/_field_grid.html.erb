<%
  grid_classes = [
    "ax-field-grid",
    "ax-field-grid--#{columns}",
    ("ax-field-grid--#{gap}" if gap.present?),
    class_name
  ].compact_blank.join(" ")
%>

<%= tag.div(class: grid_classes, data:) do %>
  <%= body %>
<% end %>
