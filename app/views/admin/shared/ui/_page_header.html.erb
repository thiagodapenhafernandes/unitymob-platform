<%# Legado: delega ao cabeçalho padrão (workspace_heading). locals: title, subtitle, icon, actions, class_name %>
<%= render "admin/shared/ui/workspace_heading", title:, subtitle: local_assigns[:subtitle], icon: local_assigns[:icon], actions: local_assigns[:actions], class_name: local_assigns[:class_name] %>
