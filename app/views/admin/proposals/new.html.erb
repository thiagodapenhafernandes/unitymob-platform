<%= ax_workspace_heading(
  eyebrow: "Comercial · Propostas",
  icon: "file-earmark-text",
  title: "Nova proposta — #{@lead.display_name}",
  subtitle: "Gere uma página compartilhável que o cliente abre no celular para aceitar ou recusar.",
  actions: ax_button("Voltar ao lead", admin_lead_path(@lead), variant: :ghost, size: :sm, icon: "arrow-left")
) %>

<%= render "form", proposal: @proposal, lead: @lead, url: admin_lead_proposals_path(@lead), method: :post, habitations: @habitations %>
