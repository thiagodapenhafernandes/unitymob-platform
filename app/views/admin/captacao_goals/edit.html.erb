<h1 class="h4 mb-3">Editar meta — <%= @goal.period_label %> · <%= @goal.kind.humanize %></h1>
<div class="card shadow-sm border-0">
  <div class="card-body">
    <%= render "form", goal: @goal %>
  </div>
</div>
