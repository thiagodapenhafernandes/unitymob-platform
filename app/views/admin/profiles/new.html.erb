<div class="mb-4">
  <%= link_to admin_profiles_path, class: "text-muted text-decoration-none small" do %>
    <i class="bi bi-arrow-left me-1"></i> Voltar para a lista
  <% end %>
  <h1 class="h3 mt-2 fw-bold text-gray-800">Novo Perfil de Acesso</h1>
</div>

<%= render 'form', profile: @profile %>
