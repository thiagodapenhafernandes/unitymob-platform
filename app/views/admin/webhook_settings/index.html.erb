<div class="container-fluid py-4">
  <!-- Header -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="mb-1"><i class="bi bi-broadcast"></i> Webhooks</h1>
      <p class="text-muted mb-0">Gerencie as integrações de envio automáticos</p>
    </div>
    <div class="d-flex gap-2">
      <%= link_to admin_root_path, class: 'btn btn-outline-secondary' do %>
        <i class="bi bi-arrow-left me-2"></i>Voltar
      <% end %>
      <%= link_to new_admin_webhook_setting_path, class: 'btn btn-primary' do %>
        <i class="bi bi-plus-lg me-2"></i>Novo Webhook
      <% end %>
    </div>
  </div>

  <div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-primary bg-opacity-10 border-0">
      <h5 class="mb-0 text-primary">
        <i class="bi bi-share-fill me-2"></i>Compartilhamento de corretores
      </h5>
    </div>
    <div class="card-body">
      <%= form_with url: share_tracking_admin_webhook_settings_path, method: :patch, local: true, class: "row g-3 align-items-end" do %>
        <div class="col-md-4">
          <%= label_tag :lead_share_tracking_days, "Validade do cookie/link (dias)", class: "form-label fw-bold" %>
          <%= number_field_tag :lead_share_tracking_days,
                               @lead_share_tracking_days,
                               min: HabitationShareLink::MIN_EXPIRATION_DAYS,
                               max: HabitationShareLink::MAX_EXPIRATION_DAYS,
                               class: "form-control" %>
        </div>
        <div class="col-md-5">
          <p class="text-muted small mb-0">
            Quando um visitante abre um link compartilhado por corretor, o site mantém essa atribuição por este período e novos leads do mesmo imóvel ficam com o corretor.
          </p>
        </div>
        <div class="col-md-3 text-md-end">
          <%= submit_tag "Salvar validade", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>

  <% if @webhook_settings.any? %>
    <div class="card shadow-sm border-0">
      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead class="bg-light">
              <tr>
                <th class="ps-4">Descrição</th>
                <th>URL do Webhook</th>
                <th>Captura Leads?</th>
                <th>Status</th>
                <th class="text-end pe-4">Ações</th>
              </tr>
            </thead>
            <tbody>
              <% @webhook_settings.each do |webhook| %>
                <tr>
                  <td class="ps-4">
                    <strong><%= webhook.description.presence || "Sem descrição" %></strong>
                    <br>
                    <small class="text-muted">ID: <%= webhook.id %></small>
                  </td>
                  <td>
                    <code class="text-primary"><%= truncate(webhook.webhook_url, length: 50) %></code>
                    <% if webhook.whatsapp_webhook_url.present? %>
                      <br>
                      <small class="text-success"><i class="fab fa-whatsapp me-1"></i><%= truncate(webhook.whatsapp_webhook_url, length: 50) %></small>
                    <% end %>
                  </td>
                  <td>
                    <% if webhook.lead_capture_enabled? %>
                      <span class="badge bg-primary bg-opacity-10 text-primary">
                        <i class="bi bi-check-circle me-1"></i>Ativa
                      </span>
                    <% else %>
                      <span class="badge bg-secondary bg-opacity-10 text-secondary">Inativa</span>
                    <% end %>
                  </td>
                  <td>
                    <% if webhook.active? %>
                      <span class="badge bg-success">Ativo</span>
                    <% else %>
                      <span class="badge bg-secondary">Inativo</span>
                    <% end %>
                  </td>
                  <td class="text-end pe-4">
                    <div class="btn-group">
                      <%= button_to test_admin_webhook_setting_path(webhook), method: :post, class: 'btn btn-sm btn-outline-info', title: 'Testar Envio', data: { confirm: 'Enviar payload de teste?' } do %>
                        <i class="bi bi-send"></i>
                      <% end %>
                      <%= link_to edit_admin_webhook_setting_path(webhook), class: 'btn btn-sm btn-outline-primary', title: 'Editar' do %>
                        <i class="bi bi-pencil"></i>
                      <% end %>
                      <%= button_to admin_webhook_setting_path(webhook), method: :delete, class: 'btn btn-sm btn-outline-danger', title: 'Excluir', data: { confirm: 'Tem certeza que deseja excluir este webhook?' } do %>
                        <i class="bi bi-trash"></i>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  <% else %>
    <div class="text-center py-5 bg-light rounded-3">
      <div class="mb-3">
        <i class="bi bi-broadcast text-secondary" style="font-size: 3rem;"></i>
      </div>
      <h3>Nenhum webhook configurado</h3>
      <p class="text-muted">Crie seu primeiro webhook para integrar com sistemas externos.</p>
      <%= link_to new_admin_webhook_setting_path, class: 'btn btn-primary mt-2' do %>
        <i class="bi bi-plus-lg me-2"></i>Criar Webhook
      <% end %>
    </div>
  <% end %>

  <div class="alert alert-info mt-4">
    <i class="bi bi-info-circle me-2"></i>
    <strong>Multicast:</strong> O sistema enviará os dados para <strong>todos</strong> os webhooks ativos listados acima simultaneamente.
  </div>
</div>
