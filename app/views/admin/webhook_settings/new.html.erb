<div class="container-fluid py-4">
  <div class="tw-flex tw-justify-between tw-items-center tw-mb-4">
    <div>
      <h1 class="ax-page-title tw-mb-1"><i class="bi bi-plus-circle"></i> Novo Webhook</h1>
      <p class="ax-page-subtitle">Adicionar nova integração</p>
    </div>
    <%= link_to 'Voltar', admin_webhook_settings_path, class: 'ax-btn ax-btn--ghost' %>
  </div>

  <%= render 'form', webhook_setting: @webhook_setting %>
</div>
