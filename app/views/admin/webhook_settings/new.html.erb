<div class="container-fluid py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="mb-1"><i class="bi bi-plus-circle"></i> Novo Webhook</h1>
      <p class="text-muted mb-0">Adicionar nova integração</p>
    </div>
    <%= link_to 'Voltar', admin_webhook_settings_path, class: 'btn btn-outline-secondary' %>
  </div>

  <%= render 'form', webhook_setting: @webhook_setting %>
</div>
