**Alert** — inline message strip for warnings, errors and confirmations inside a page or module.

```jsx
<Alert tone="warning" title="Módulo Campo desativado">
  Ative somente quando a operação externa estiver em uso.
</Alert>
```

Tones: `default · danger · warning · success`. Icons default per tone but can be overridden.
