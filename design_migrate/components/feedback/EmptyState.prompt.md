**EmptyState** — centered placeholder when a list, board column or panel has no data.

```jsx
<EmptyState icon="kanban" title="Nenhuma pendência" action={<Button size="sm">Criar tarefa</Button>}>
  Tudo em dia por aqui.
</EmptyState>
```
