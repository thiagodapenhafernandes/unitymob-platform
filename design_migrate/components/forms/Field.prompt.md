**Field** — wraps a control with an `.ax-label`, optional required asterisk and hint text. Compose around Input/Select/Textarea.

```jsx
<Field label="Preço" htmlFor="price" required hint="Somente números">
  <Input id="price" placeholder="R$ 0,00" />
</Field>
```
