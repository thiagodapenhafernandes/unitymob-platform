**Card** — the base white panel used for every module surface. Optional header with `eyebrow`, `title` and an `actions` slot.

```jsx
<Card eyebrow="Funil" title="Conversão comercial" actions={<Button size="sm" icon="download">Exportar</Button>}>
  …conteúdo…
</Card>
```
